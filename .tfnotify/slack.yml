---
ci: circleci
notifier:
  slack:
    token: $SLACK_TOKEN
    channel: $SLACK_DEFAULT_CHANNEL
    bot: $SLACK_BOT_NAME
terraform:
  plan:
    template: |
      {{ .Message }}
      {{if .Result}}
      ```
      {{ .Result }}
      ```
      {{end}}
      ```
      {{ .Body }}
      ```