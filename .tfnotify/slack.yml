---
ci: circleci
notifier:
  slack:
    token: $SLACK_ACCESS_TOKEN
    channel: $SLACK_DEFAULT_CHANNEL
    bot: "test-name"
terraform:
  plan:
    template: |
      {{ .Message }}
      {{if .Result}}
      ```
      {{ .Result }}
      ```
      {{end}}
      ```
      {{ .Body }}
      ```